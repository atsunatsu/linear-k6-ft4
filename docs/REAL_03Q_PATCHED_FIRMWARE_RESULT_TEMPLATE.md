# Patched Real 0.3q Combined Test Result Template

## Chinese / 中文

### 1. 基本信息
- 测试日期:
- 测试人:
- 固件文件名:
- DigiManager 文件名:
- 是否全程假负载或断开天线: `yes / no`

### 2. 刷机后基础状态
- 能否正常开机: `yes / no`
- 开机版本显示:
- 菜单是否仍像真实 `0.3q`: `yes / no`
- 数字模式入口是否仍存在: `yes / no`

### 3. DigiManager 替换后状态
- patched DigiManager 能否启动: `yes / no`
- DigiManager 主界面是否基本正常: `yes / no`

### 4. FT8 回归
- FT8 是否仍能发射: `yes / no`
- DigiManager 是否有正常反应: `yes / no`
- 电台是否有正常 TX 迹象: `yes / no`

### 5. FT4 发射
- FT4 是否真正进入发射: `yes / no`
- DigiManager 是否有反应: `yes / no`
- 电台是否有 TX 迹象: `yes / no`

### 6. 数字模式改频
- 空闲态改频是否生效: `yes / no / temporary`
- TX 态改频是否生效: `yes / no / temporary`
- 如果不是稳定生效，请说明是完全不变，还是短暂变化后拉回:

### 7. 截图和补充说明
- 开机界面截图:
- 菜单截图:
- DigiManager 主界面截图:
- 其他异常现象:

## English

### 1. Basic Info
- Test date:
- Tester:
- Firmware filename:
- DigiManager filename:
- Dummy load or antenna disconnected the whole time: `yes / no`

### 2. Basic Boot State
- Normal boot: `yes / no`
- Boot version shown:
- Menu still looks like real `0.3q`: `yes / no`
- Digital-mode entry still exists: `yes / no`

### 3. DigiManager Replacement State
- Patched DigiManager starts normally: `yes / no`
- Main window still looks basically normal: `yes / no`

### 4. FT8 Regression
- FT8 still transmits: `yes / no`
- DigiManager reacts normally: `yes / no`
- Radio shows normal TX behavior: `yes / no`

### 5. FT4 TX
- FT4 really starts transmitting: `yes / no`
- DigiManager reacts: `yes / no`
- Radio shows TX behavior: `yes / no`

### 6. Digital-Mode Retune
- Idle retune works: `yes / no / temporary`
- TX retune works: `yes / no / temporary`
- If not stable, describe whether it never changed or changed briefly and snapped back:

### 7. Screenshots / Notes
- Boot screen screenshot:
- Menu screenshot:
- DigiManager screenshot:
- Any other abnormal behavior:

