# Patched Real 0.3q Firmware Result Template

## 中文
请尽量按下面格式回报：

### 1. 基本信息
- 测试日期：
- 测试人：
- 使用固件文件：
- 是否全程假负载或断开天线：`yes / no`

### 2. 刷机后基础状态
- 能否正常开机：`yes / no`
- 开机版本显示：
- 菜单是否仍像真实 `0.3q`：`yes / no`
- 数字模式入口是否存在：`yes / no`

### 3. FT8 回归
- FT8 是否还能发射：`yes / no`
- DigiManager 是否有正常反应：`yes / no`
- 电台是否有正常发射迹象：`yes / no`

### 4. 数字模式改频
- 空闲态改频是否生效：`yes / no / temporary`
- TX 态改频是否生效：`yes / no / temporary`
- 如果无效，是完全不变，还是短暂变化后拉回：

### 5. FT4 发射
- FT4 是否真正进入发射：`yes / no`
- DigiManager 是否有反应：`yes / no`
- 电台是否有发射迹象：`yes / no`
- FT4 测试时频率是否变化：`yes / no / temporary`

### 6. 截图或补充说明
- 开机界面截图：
- 菜单截图：
- DigiManager 截图：
- 其他异常现象：

## English

### 1. Basic Info
- Test date:
- Tester:
- Firmware file used:
- Dummy load or disconnected antenna the whole time: `yes / no`

### 2. Basic Boot State
- Normal boot: `yes / no`
- Boot version shown:
- Menu still looks like real `0.3q`: `yes / no`
- Digital-mode entry still exists: `yes / no`

### 3. FT8 Regression
- FT8 still transmits: `yes / no`
- DigiManager reacts normally: `yes / no`
- Radio shows normal TX behavior: `yes / no`

### 4. Digital-Mode Retune
- Idle retune works: `yes / no / temporary`
- TX retune works: `yes / no / temporary`
- If not stable, describe whether it never changed or changed briefly and snapped back:

### 5. FT4 TX
- FT4 really starts transmitting: `yes / no`
- DigiManager reacts: `yes / no`
- Radio shows TX behavior: `yes / no`
- Frequency changes during FT4 test: `yes / no / temporary`

### 6. Screenshots / Notes
- Boot screen screenshot:
- Menu screenshot:
- DigiManager screenshot:
- Any other abnormal behavior:
